import type { Task } from '../types/task'
export function TaskSummary({ tasks }: { tasks: Task[] }) { const completed = tasks.filter((task) => task.status === 'completed').length; const items = [{ label: 'Total tasks', value: tasks.length, type: 'total' }, { label: 'Pending', value: tasks.length - completed, type: 'pending' }, { label: 'Completed', value: completed, type: 'completed' }]; return <section className="summary-grid" aria-label="Task summary">{items.map((item) => <article className={`summary-card ${item.type}`} key={item.label}><p className="summary-label"><span className="summary-dot" />{item.label}</p><strong className="summary-count">{item.value}</strong></article>)}</section> }
